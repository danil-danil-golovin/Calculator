package com.example.Calculator;

public interface CalculatorService {
     String greating();

     Float plus(Float one, Float two);

     Float minus(Float one, Float two);

     Float multiply(Float one, Float two);

     Float divide(Float one, Float two);
}

